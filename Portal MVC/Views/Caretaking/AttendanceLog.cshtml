@using Portal_MVC.Models
@model Portal_MVC.Models.AttendanceLogViewModel
@{
    ViewBag.Title = "AttendanceLog";
    Layout = "~/Views/Shared/_HomeLayout.cshtml";
}

@using (Html.BeginForm("MyAccount", "Home", FormMethod.Post))
{
    <div class="container">
        <div class="row">
            <div class="form-group">
                @Html.DropDownListFor(m => m.EstateList, new SelectList(Model.EstateList,"id",
               "Address1" ), new { @class = "form-control" })
            </div>

            <div class="form-group">
                @*<label class="">
                    Caretaking Visit
                    @Html.CheckBoxFor(m => m.CaretakingVisit, new { @class = "form-control" })
                    <span class="checkmark"></span>
                </label>
                <label class="">
                    Inspection Visit
                    @Html.CheckBoxFor(m => m.CaretakingVisit, new { @class = "form-control" })
                    <span class="checkmark"></span>
                </label>*@
                <label class="">
                    Call Out Visit
                </label>
                @Html.CheckBoxFor(m => m.CaretakingVisit, new { @class = "form-control" })
            </div>
        </div>
    </div>
}

